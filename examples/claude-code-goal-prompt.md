# Claude Code `/goal` prompt

Generate the concrete invocation with `/parallel-slices-prepare`. The JSON run
state must name `claude-code`; the repository default may name any enabled tool.

```text
/goal Complete <EXACT_MILESTONE_AND_FINAL_SLICE> from <PLAN_PATH>, using <RUN_STATE_PATH> as durable JSON state, on <GOAL_BRANCH>, as controller claude-code. Invoke /parallel-slices-next for scheduling and worker events. Act only as the root orchestrator: never implement slice code in this conversation. Recompute readiness from committed manifests and state, create one managed worktree per Ready Slice, and spawn one fresh background subagent for each returned worktree without creating a second native worktree. Parallelize only the returned Ready Slices. As each worker finishes, verify its tracked gate and candidate commit from Git evidence; if its dependencies are accepted, atomically claim the goal checkout and integrate, review, state-record, and commit that candidate serially without waiting for unfinished independent workers. Recompute readiness after every accepted slice and start newly unlocked non-conflicting workers immediately. Preserve every plan invariant, existing integration, and explicit non-goal. Run each exact pipeline and produce required developer release fragments. Follow .parallel-slices/repository.json. In GitHub mode, after the one final audit push only <GOAL_BRANCH>, create or update one complete-goal pull request, and monitor CI until green; PULL_REQUEST_READY completes the goal. In local-only mode, do not contact a remote; MILESTONE_FINISHED completes the goal. SLICE_ACCEPTED means the goal remains active. BLOCKED and FAILED are terminal impasses. Never create a PR per slice, merge, push a protected branch, deploy, publish, change repository settings, contact unrelated external systems, or run production migrations. Do not continue into <EXCLUDED_LATER_PHASES_OR_WORK>.
```
