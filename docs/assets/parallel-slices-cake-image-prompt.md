# Parallel Slices cake diagram prompt

Use this single prompt to generate the cake-metaphor overview image for
Parallel Slices. Generate a new image from scratch. Do not edit, extend, or use
an earlier generated image as a reference. Save the approved image as
`docs/assets/parallel-slices-cake-generated.png`.

```text
Create a polished, high-resolution editorial infographic on a warm ivory 3:2 landscape canvas. Use crisp charcoal typography, restrained blue, orange, teal, green, and berry-red accents, subtle texture, and friendly AI pastry-chef robots. Keep all content inside a 6% safe area. Use three spacious horizontal rows: a compact planning row across the top, a concurrent-worker row across the middle, and a serial-assembly row across the bottom. The serial assembly must be completely below all three worker lanes, never beside them. Draw clean connectors that never cross cards or text. No logos, watermarks, photorealism, round cakes, wedges, or green food.

Accuracy priority, highest to lowest: exact connector topology; exact six-cell unfinished and finished cake geometry; exact labels; decorative robot detail. If space is tight, make robots and icons smaller. Never simplify, reroute, duplicate, or omit a node, cell, slice, or required connector.

VISIBLE TITLE
Plan the Product. Build It in Parallel Slices.

VISIBLE SUBTITLE
Approve the recipe once. Each finished slice enters root quality immediately while other AI workers keep baking.

Render only visible labels explicitly written in this prompt. Do not render instruction headings or sentences beginning with “Visual:”.

CAKE GEOMETRY

- The product is one long rectangular layer cake made from exactly six equal thin vertical slices on one rectangular board.
- Every slice is a tall narrow upright rectangular slab exactly three times taller than its visible width. Six adjacent slices therefore form a complete cake whose body is about twice as wide as it is tall. All slices have identical dimensions and aspect ratio everywhere they appear after BAKE.
- Each solid slice has at least five crisp horizontal cake-and-filling layers.
- A is blueberry-blue and cream. B is citrus-orange and vanilla. C is berry-red and cream. Preserve each slice's exact color, layers, size, and orientation everywhere it reappears.
- Before BAKE use only paper recipes and dotted wireframes. Solid edible cake first appears after BAKE.

TOP ROW

Use exactly four columns, never five. Column two contains two equal cards stacked vertically in precisely the same x-range: PRODUCT PLAN above ARCHITECTURE PACKAGE. Their left edges, right edges, and widths match. ARCHITECTURE PACKAGE must not appear as a separate third column. Column three is AI RECIPE COMPILER. Column four is OPTIMIZED SLICE MAP.

PRODUCT DIRECTION
Human + AI define requirements

Visual: a human and AI assistant collaborate on blank recipe notes. No cake.

PRODUCT PLAN
Requirements · Decisions · Evidence · Non-goals
HUMAN APPROVED

Visual: an approved paper recipe. The approval stamp appears only here.

ARCHITECTURE PACKAGE
Repository · Contracts · Quality floors

Visual: a technical blueprint. No cake.

AI RECIPE COMPILER
Approved plan + architecture → optimized slices

Visual: a robot combines the two paper inputs into a schematic.

OPTIMIZED SLICE MAP
Slices · Dependencies · Paths · Locks

Visual: one dotted rectangular plan divided into exactly six equal numbered cells. Mark A in cell 1, B in cell 3, and C in cell 5. No food.

Inside the existing OPTIMIZED SLICE MAP card, add one very small muted blue-gray capability cue along its bottom edge: two or three overlapping outline reviewer avatars followed by the micro-caption MULTI-AGENT PLAN REVIEW AVAILABLE. Keep this cue visually secondary to the map title and contents. It subtly indicates that the phase supports multi-agent review without presenting that capability as another required diagram node; it is not another card, decision, or flow step, and it has no connector. Do not enlarge or reposition the map card to accommodate it.

Connect PRODUCT DIRECTION only to PRODUCT PLAN. PRODUCT PLAN and ARCHITECTURE PACKAGE merge into AI RECIPE COMPILER. The compiler connects to OPTIMIZED SLICE MAP.

OPTIMIZED SLICE MAP has exactly one outgoing blue connector. Start it at the map's bottom-center, descend only into the UPPER HALF of the empty gap between the top and middle rows, turn left across that upper routing band until directly above ROOT CHEF, then turn down. Its one plain arrowhead must visibly touch ROOT CHEF's top-center border. This blue route terminates at ROOT CHEF. It must not continue beyond ROOT CHEF, branch, share a segment or junction with another connector, point to the scheduling fork, point to a worker lane, point to the streaming caption, or reach the serial card. Preserve a conspicuous uninterrupted ivory gap below the long horizontal part of this blue route.

MIDDLE ROW

Use a disciplined horizontal grid. ROOT CHEF occupies approximately x=2%–15%. Three narrow equal worker columns occupy the compact x=17%–52% band: A around x=17%–28%, B around x=29%–40%, and C around x=41%–52%. The right edge of the SLICE C worker column must end before x=52%. Reserve x=65%–94% for the plain streaming caption and later right-side completion cards. Do not extend any worker column into that reserved right-side column.

ROOT CHEF
Orchestrates Ready Slices

Visual: Root Chef holds scheduling cards. Root Chef does not bake or assemble cake.

READY SLICES — BAKE CONCURRENTLY

Render READY SLICES — BAKE CONCURRENTLY as a small plain unboxed caption centered above the worker columns, not as a card, badge, port, shield, or endpoint object.

Fork ROOT CHEF into three equal isolated worker columns arranged side by side at the same height. Draw one clear PURPLE scheduling route that originates only at ROOT CHEF's UPPER-RIGHT BORDER. Beginning exactly on that border, draw one short purple elbow upward and then right into one uninterrupted horizontal purple distributor above A, B, and C. The left endpoint of this distributor must be visibly joined to the elbow from ROOT CHEF; it may never float or begin above SLICE A. From that one distributor, draw exactly three separate vertical purple branches ending with ordinary plain triangular purple arrowheads that touch the TOP borders of SLICE A, SLICE B, and SLICE C respectively. These are the only three purple arrowheads. All three workers must visibly receive an incoming purple arrow from ROOT CHEF's connected distributor. No purple line or arrow may leave ROOT CHEF's right-center or lower-right border, and no arrow may enter the left or right side of any worker card. In particular, never draw a direct horizontal arrow from ROOT CHEF into SLICE A's left border. The purple distributor is only a bare continuous line, not a bar, card, socket, shield, badge, or container.

Keep the complete purple scheduling route visibly below and separate from the blue OPTIMIZED SLICE MAP route. Leave a broad ivory gap between them. The blue and purple lines never touch, cross, overlap, merge, share a junction, or share a segment. No purple scheduling line originates at OPTIMIZED SLICE MAP. No blue map line reaches a worker. This purple input fork is also separate from the green candidate outputs and does not imply a completion barrier. Each column is a compact top-to-bottom sequence, so the three CANDIDATE SLICE trays finish on the same baseline at three different x-coordinates:

SLICE A
BAKE ↓ SLICE QUALITY CHECK ↓ CANDIDATE SLICE

SLICE B
BAKE ↓ SLICE QUALITY CHECK ↓ CANDIDATE SLICE

SLICE C
BAKE ↓ SLICE QUALITY CHECK ↓ CANDIDATE SLICE

Visual: each column begins with a paper ticket, then a small baker actively uses an oven at BAKE, checks one solid thin multilayer slice standing upright, and leaves that same upright slice on a candidate tray at the column's bottom. The slice shown at SLICE QUALITY CHECK must not be horizontal, wide, rotated, or block-shaped. Keep all three columns equal in width and height.

Fresh context · Isolated worktree · One scoped feature

STREAMING OPTIMIZATION

Do not join the three worker columns. Do not draw a holding-set card, queue, batch barrier, or ROOT-VERIFIED CANDIDATE SLICES card. Each CANDIDATE SLICE has its own green connector with one plain arrowhead touching a different point on the top border of SERIAL QUALITY + ASSEMBLY. The three connectors remain separate; no horizontal bus connects them.

Place SERIAL QUALITY + ASSEMBLY in the bottom row, completely below the worker columns. Its left border is near x=5% and its right border is near x=56%. The serial card's right border must be visibly to the right of the SLICE C candidate tray and C's vertical connector. Draw exactly three short, separate, straight vertical teal connectors:

- The connector from A starts at A's candidate tray bottom-center and descends into the left serial-card top endpoint.
- The connector from B starts at B's candidate tray bottom-center and descends into the center serial-card top endpoint.
- The connector from C starts at C's candidate tray bottom-center and descends into the right serial-card top endpoint.

The three short vertical shafts are parallel and widely separated. Each connector has exactly one arrowhead, located only where it touches the serial card's top border. Do not draw horizontal green output lines from candidates. Do not draw any extra arrows from the streaming caption. No worker connector may enter the serial card through its left or right border, continue past the serial card, or enter FINAL GOAL AUDIT.

Do not draw connector ports, sockets, shields, shield outlines, badges, pentagons, tabs, plugs, endpoint markers, or any other object where an arrow meets ROOT CHEF or SERIAL QUALITY + ASSEMBLY. The last segment of every connector is a bare line followed by one ordinary triangular arrowhead touching the destination card border directly, with no enclosing shape.

Keep the entire vertical gap between each CANDIDATE SLICE tray and the serial card free of text, icons, captions, and horizontal lines. Each of the three vertical connectors must continue uninterrupted through that gap until its arrowhead touches the serial card's top border.

Render this as plain unboxed caption text in the reserved right-side column directly beneath STREAMING OPTIMIZATION, not between the candidates and serial card:

EACH FINISHED SLICE ENTERS IMMEDIATELY
Other workers keep baking · No batch wait

The root admits a slice only after its tracked worker quality gate and candidate checkpoint pass. If its recipe dependencies are accepted, the root atomically claims the one serial assembly slot, places that slice, and runs integrated quality while sibling bakers continue.

BOTTOM ROW

Place one wide green card across the lower left and center, completely below all worker lanes. Its left border begins at x=5% and its right border ends near x=56%, to the right of all three worker input arrows. Keep the outer gutter from x=1% to x=4% completely empty except for the two return routes, so those routes can reach ROOT CHEF without touching the serial card:

SERIAL QUALITY + ASSEMBLY
ROOT VERIFY + PLACE + QUALITY CHECK
Integrated quality pipeline · Independent taste review

The serial card has one closed rounded-rectangle border. Inside this card draw one shared cake assembly and one checklist/reviewer group. Do not number process steps and do not duplicate the cake. Reserve the clean x=58%–64% area immediately to the right of the closed serial card for QUALITY + REVIEW PASS?. The serial card has exactly one outgoing connector: one short, straight, perfectly horizontal deep-green arrow that begins at the card's right-center border and ends with one ordinary triangular arrowhead touching the left point of QUALITY + REVIEW PASS?. Keep the decision diamond's vertical center aligned with the serial card's right-center. Do not extend the card border into this arrow, and do not put a port, socket, shield, badge, pentagon, tab, or endpoint marker at either end. QUALITY + REVIEW PASS? has exactly one incoming connector, and it comes only from SERIAL QUALITY + ASSEMBLY. No worker candidate connector may point directly to the decision diamond.

SIX-CELL ASSEMBLY

Use a straight-on front view of one compact rectangular dotted wireframe on one board. The outer cake frame is exactly twice as wide as it is tall. It is exactly one outer rectangle with exactly five internal vertical dividers, creating exactly six equal tall narrow cells; every cell is three times taller than it is wide. Do not stretch this frame across the available card width—preserve the 2:1 outer aspect ratio and leave whitespace beside it. Render plain numbers 1 through 6 in a clear row immediately below the cells, one centered under each cell, so the moving C and baker cannot cover a number.

Lay out the six cells before adding any food. The frame has exactly seven vertical boundary lines: left outer edge, five dividers, right outer edge. Every cell has exactly one number beneath it in the uninterrupted sequence 1, 2, 3, 4, 5, 6; there is no unnumbered gap or cell. Cell 5 is the second-to-last cell. Cell 6 is the last cell at the far right and stays empty. Never create a seventh cell to make room for the baker or C, and never place C in the last cell.

Treat this bracket notation as the mandatory solid content inside the unfinished wireframe board, not as visible text:

[1:BLUE A][2:EMPTY][3:ORANGE B][4:EMPTY][5:EMPTY][6:EMPTY]

- Cell 1 contains one solid blue A, fully seated and exactly filling cell 1 from its left boundary to its right boundary.
- Cell 2 is empty dotted wireframe.
- Cell 3 contains one solid orange B, fully seated and exactly filling cell 3 from its left boundary to its right boundary.
- Cell 4 is empty dotted wireframe.
- Cell 5 remains an open dotted wireframe slot with no seated solid. After completing the six-cell board with only A and B inside it, draw the one and only solid red C in the ivory space immediately above open cell 5. The vertical centerline of C, the centerline of open cell 5, and the printed number 5 below that cell form one straight vertical line. From C's right edge to the board's right outer edge there is exactly one cell width: the complete empty cell 6. There must not be two empty cells to C's right, and C must not align with cell 4 or the divider between 4 and 5. C's bottom edge touches the top opening of cell 5 but does not obscure the empty cell below. C has exactly the same width, height, orientation, and layer thickness as A and B. A small baker stands directly behind cell 5 with both hands physically touching the top corners of this same C and lowers it toward the open slot without covering either boundary of cell 5 or any part of cell 6. Show one short downward arrow centered on C. Do not draw any other slice in the baker's hands or any second red C.
- Cell 6 is empty dotted wireframe and remains fully visible immediately to the right of C.

Critical C rule: complete the board first with this six-cell solid-content sequence: BLUE, BLANK, ORANGE, BLANK, BLANK, BLANK. Then overlay exactly one red C above the already drawn fifth cell without changing, splitting, or adding any cell. Count solid red cake objects inside SERIAL QUALITY + ASSEMBLY before finishing: exactly ONE. There is no seated red cake anywhere in the unfinished board. The entire open dotted cell 5 remains visible below the moving C. If a cell has no number or if a seventh cell appears, the diagram is incorrect.

The green serial card contains exactly six wireframe cells and exactly three solid cake objects total: seated A in cell 1, seated B in cell 3, and moving C above open cell 5. There is no seventh cell. There is no duplicate C. Number 5 is directly above the open target slot under C; number 6 is directly above the final fully visible empty cell to C's right. A, B, and C each equal one complete cell width. All three solids are equally thin and upright.

To the left of this same cake, show one green root-verification checklist, one compact integrated-quality checklist, and one small reviewer baker pointing a magnifying glass rightward toward the single moving C above cell 5. Add two tiny overlapping outline reviewer avatars beside that reviewer and the muted micro-caption MULTI-AGENT SLICE REVIEW AVAILABLE. Treat the avatars and caption only as a subtle capability cue inside the existing reviewer group; they are not extra process cards, do not inspect separate cake objects, and have no connectors. Keep the entire area immediately to the right of cell 5 reserved for the fully visible empty cell 6; never place the reviewer there. The reviewer has empty hands except for the magnifying glass. Do not place any cake, slice, tray, plate, wireframe, or food beside the reviewer. These symbols inspect C and never introduce another cake or slice.

Immediately to the right of the closed green serial card, fully outside its border around x=58%–64%, draw:

QUALITY + REVIEW PASS?

Its YES arrow is a short horizontal arrow pointing right into one small green rounded acceptance pill. The pill is a real visible node, not a connector label, and contains exactly these two lines:

SLICE ACCEPTED
Recompute readiness

One short green arrow leaves the pill and connects to the decision diamond immediately beside it:

MORE SLICES RUNNING OR REMAIN?

The QUALITY + REVIEW PASS? diamond's NO arrow is one continuous vertical red line pointing straight down. Place the retry card directly beneath this diamond on the same vertical centerline. The red arrowhead visibly touches the retry card's top border. Render NO once beside this line.

FRESH RETRY — SAME SLICE
New worktree · Same recipe ticket

Visual: a fresh baker, one paper ticket, and one rejected solid slice on a tray.

Draw exactly one FRESH RETRY — SAME SLICE card in the entire image. Do not repeat, mirror, summarize, or illustrate this retry as a second card anywhere else.

The retry card has exactly one incoming red connector from QUALITY + REVIEW PASS? and exactly one outgoing red connector. Its outgoing connector starts at the retry card's bottom-left corner, descends first into a dedicated ivory corridor below the bottom border of SERIAL QUALITY + ASSEMBLY, then turns left without an arrowhead and travels through that bottom corridor. Never draw a short left-pointing arrow from the retry card at the card's own height; that would incorrectly point into the serial card. Keep a clearly visible ivory gap between the bottom horizontal red line and the entire green serial-card border. At x=4% in the reserved outer gutter, the red line turns upward without an arrowhead and rises entirely to the left of the serial card. At the height of ROOT CHEF's lower-left border, it turns right and ends with its only plain arrowhead visibly touching that border. It must reach ROOT CHEF; it must not stop below the root card, at the serial-card border, ROOT VERIFICATION checklist, taste reviewer, or any serial-card content. No red connector may start from the retry card's right edge. The red route has no arrowhead in the bottom corridor or at either turn. Render this caption horizontally above the bottom red segment around x=18%–42%, never beside or across the vertical red segment:

Only the failed slice retries; accepted slices stay in the product cake

MORE SLICES RUNNING OR REMAIN? is mandatory. Place it immediately to the right of the small SLICE ACCEPTED pill at the same vertical level, fully outside the serial card around x=74%–81%, with a visible ivory gap between the flow nodes and the serial card's closed right border. It can only be reached by the one short green connector from SLICE ACCEPTED. No line or arrow may originate from the serial-card border. Its YES arrow leaves from the bottom point of the diamond, descends to the bottom safety corridor, travels all the way left below the retry card and serial card, turns upward at x=3% in the reserved outer gutter, and rises entirely to the left of both the serial card and the red route. At the height of ROOT CHEF's upper-left border, it turns right and ends with its only plain arrowhead visibly touching that border. It must reach ROOT CHEF and must not stop at the serial-card border or a checklist. The green route has no arrowhead in the bottom corridor or at its upward turn. This is one large outer green loop labeled:

NEXT READY SLICES

The NEXT READY SLICES loop has exactly one destination: the upper-left border of ROOT CHEF. The red retry route has exactly one destination: a separate lower-left point on ROOT CHEF. Their long horizontal bottom segments have no arrowheads and may not stop in the middle of the canvas. The green and red routes never merge or change colors. Neither route may stop below ROOT CHEF, touch the serial card, or end near MORE SLICES RUNNING OR REMAIN?.

Place FINAL GOAL AUDIT immediately to the right of MORE SLICES RUNNING OR REMAIN? at exactly the same vertical center in the dedicated x=83%–96% completion column. Lower the audit card as needed to align it with the diamond. Draw one short perfectly horizontal deep-green arrow from the diamond's right-side NO output, with its arrowhead touching the audit card's left-center border. The printed NO label, the entire connector line, and its arrowhead are deep green, never red, orange, pink, or berry. This NO connector must not bend downward or touch GOAL COMPLETE.

FINAL GOAL AUDIT
Once after all six slices are accepted
Requirements · Preservation · Gates · Reviews · Evidence · State · Non-goals

Place GOAL COMPLETE directly below FINAL GOAL AUDIT. Draw one deep-green vertical arrow from FINAL GOAL AUDIT into GOAL COMPLETE. GOAL COMPLETE has exactly one incoming connector, and it comes from FINAL GOAL AUDIT. This entire successful completion route—MORE SLICES RUNNING OR REMAIN? NO, its arrow to FINAL GOAL AUDIT, and the arrow from FINAL GOAL AUDIT to GOAL COMPLETE—is deep green. Never connect a worker lane, serial input connector, QUALITY + REVIEW PASS?, FRESH RETRY, or MORE SLICES RUNNING OR REMAIN? directly to GOAL COMPLETE.

Do not swap, skip, merge, duplicate, or reorder the bottom-row nodes described above.

GOAL COMPLETE
One goal branch · Separate slice commits · One pull request when GitHub-enabled

Visual: one complete long rectangular cake made from exactly six separate equal thin upright slices. Its cake body is exactly twice as wide as it is tall, because each of its six slices is exactly three times taller than wide. Render plain numbers 1 through 6 above them. The exact left-to-right order is: blue A in 1; cream-chocolate in 2; orange B in 3; vanilla-purple in 4; red C in 5; vanilla-chocolate in 6. All six slices have identical width, height, orientation, and layer thickness. A, B, and C match the serial assembly exactly in aspect ratio, thinness, layers, color, and numbered position; C is fully seated here but is not resized. Keep visible seams between all six slices. Do not draw seven slices, three thick blocks, wireframe, empty cells, round cake, or differently sized slices.

Delivery is architecture-owned and separately authorized.

FINAL CONNECTOR CHECK

- The top row has four columns; Product Plan and Architecture Package are stacked in the same second-column x-range and merge into the compiler.
- The small multi-agent review cues stay inside OPTIMIZED SLICE MAP and the existing serial-card reviewer group. They are capability annotations only, with no connectors, added nodes, added columns, or layout expansion.
- Three workers enter SERIAL QUALITY + ASSEMBLY independently as each finishes; they never join or wait for one another.
- The blue OPTIMIZED SLICE MAP route ends only at ROOT CHEF. It never reaches the scheduling distributor or a worker.
- A separate purple scheduling route originates only at ROOT CHEF and feeds all three worker columns A, B, and C with plain arrowheads touching their top borders.
- The purple distributor is visibly connected to ROOT CHEF's upper-right border and never floats. No direct horizontal arrow enters SLICE A's left border.
- A broad visible ivory gap separates the blue map route from the purple scheduling route; those routes never touch, cross, merge, or share any segment or junction.
- SERIAL QUALITY + ASSEMBLY is the only destination of the three worker output connectors, and every connector ends with a plain arrowhead directly on the card border.
- In particular, the SLICE C connector ends on the serial card's top border and must never point to QUALITY + REVIEW PASS?.
- The streaming caption has no outgoing arrows. Only the three Candidate Slice connectors enter the serial card.
- SERIAL QUALITY + ASSEMBLY has exactly one outgoing deep-green arrow from its right-center border to the left point of QUALITY + REVIEW PASS?. That decision diamond has exactly one incoming connector, only from the serial card.
- QUALITY + REVIEW PASS? reaches SLICE ACCEPTED only after pass and reaches FRESH RETRY — SAME SLICE only after failure.
- SLICE ACCEPTED reaches MORE SLICES RUNNING OR REMAIN?. Exactly one connector enters that decision: the connector from SLICE ACCEPTED. The closed serial-card border never becomes a second connector.
- There is exactly one FRESH RETRY — SAME SLICE card.
- FRESH RETRY — SAME SLICE returns only to ROOT CHEF and has no connector to the serial card.
- MORE SLICES RUNNING OR REMAIN? YES returns only to ROOT CHEF; NO reaches only FINAL GOAL AUDIT.
- FINAL GOAL AUDIT reaches only GOAL COMPLETE, and no connector bypasses it.

FINAL CAKE COUNT CHECK

- Unfinished assembly: exactly six equal cells numbered 1–6; solid A seated in 1, solid B seated in 3, open cell 5 directly under the one moving C, and empty cell 6 visibly to C's right.
- Exactly one empty cell—cell 6—appears to the right of moving C. C is centered over number 5, never number 4 or a divider.
- A, B, and C each occupy exactly one equal cell width; C is never wider than A or B.
- Finished cake: exactly six equal thin solid slices in the same numbered positions, with A, B, and C matching the unfinished assembly.
```
